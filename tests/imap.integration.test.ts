beforeAll(async () => {
});

beforeEach(() => {
});

afterEach(async () => {
});

describe("Integration Tests for IMAP Actions", () => {

    // @TODO Mock mail server

    it("should authenticate successfully", async () => {
    });
});
